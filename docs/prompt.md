- コマンドボタン毎に編集可能にしたいんだ
  - コマンドボタンのタイトル変更
- コマンドボタンのドロップダウン項目の追加
  - コマンドと説明
- 履歴一覧も参照可能にして、クリックで再実行可能にしたいんだ 
- ちょっと違うんだ。オリジナルのコマンドを作成可能にしたいんだ。例えば、MyCommand を作成して、ぶら下がるコマンドを自由に登録していける形。既存のコマンドも編集可能にしたい。だから、右サイドメニューではコマンドを編集しづらいから別画面で行えるようにするとかかな
- 最前面化も可能なら実装してほしい。
- ウィンドウサイズや位置の維持も可能なら実装してほしい
- 低頻度の more は削除してほしい
- コマンドは JSON（またはMD？）で編集可能にして、保存と読み込みを可能としたい。保存はファイル保存、読み込み後は永続化（または常時参照）
- コマンドは 並び順を指定したい（並び順じゃなくても JSON の順番通りとか）
- 最前面に固定が効果ないんだ
- Fall Out っていうゲームの配色が選択可能にしたいなあ。伝わるかな？
- 添付画像の部分のUIが重なってるから修正してほしい
- 設定ではなく、コマンド編集可能なコマンドはコマンド管理から表示切替の設定を可能としたい。設定から個別のコマンド表示切替を削除してすっきりしたい。
- 設定からコマンド履歴は除外してほしい。
- Fallout ではすべての文字色を変更可能にしてほしい
- 下記のコマンドも追加してほしいな。ドロップダウンで option 付きが選択可能にしてほしい
  - ipconfig
  - net
  - tracert
- グループ表示は一覧ではなくて、選択しているコマンド毎で設定するようにしたいんだ。
- 全てのボタンには Tooltip で説明文を追加してほしい。リセットボタンではどこまでリセットされるのかとかも考慮して。
- 文字色、背景色を自由に選択可能にしたい。
- README.md を作成して、使用方法など記載してほしい。
- コミットしてプッシュしてほしい。github リポジトリはまだ未作成で、BuildCooperWorks の組織にプッシュしたいんだ
  - https://github.com/BuildCooperWorks
- /BcwTerminal-0.1.0-portable.exe を起動すると、スマートアプリコントロールが～っでブロックされて起動できないんだ
- スマートアプリコントロール～をオフにしないと動作しないってREADMEにかいてほしい。また、ＯＮの場合検知して、メッセージ表示をアプリで表示できたりする？
- Windows では ls ではなくて dir だから dir コマンドを追加してほしいんだ。ls は規定で非表示。dir は表示。
- 右側のセッション一覧のバツボタンを押しても、停止中のまま動作しなくなるから修正してほしい。
- npm run dev 前までは正常に起動できていたのに、突然起動しなくなった感じがするんだけど、原因はなんだったの？
- やっぱり electron はファイルサイズが大きくなるの？exe が 100MB近くあるんだ。
- とりあえず、配布元は google drive にしようと思うんだ。exe 直ダウンロードは厳しいよね？
- 自動バージョンアップは難しいのかな？
- ディレクトリ操作系のコマンドを追加したい。mkdir とか cd とか
- 配布は Google Drive でダウンロードにしているんだけど、ダウンロード解凍して実行するとブロックされるんだ。他に良さげな配布方法はないかなあ？
- mkdir new-folder だとフォルダー名は指定できないよね？
- プロジェクトページを開くのリンク先はこちら「https://github.com/BuildCooperWorks/bcw-terminal」
- 全画面にするとメインメニューが消えて戻せなくなる。全画面の時だけ戻すわかりやすいボタンを配置してほしいな。